apiVersion: v1
kind: Service
metadata:
  name: flask-backend-service
  namespace: devops-platform
  labels:
    app: flask-backend
spec:
  type: ClusterIP
  selector:
    app: flask-backend
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP